#!/usr/bin/env bash

#(ng lint && cd functions && webpack && firebase deploy --only functions)
# (tslint --fix "functions/src/**/*.ts" && cd functions && webpack && firebase deploy --only functions)
(cd functions && webpack && firebase deploy --only functions)
